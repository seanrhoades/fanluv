<%= form_for(current_fan.luvs.build) do |f| %>
 <%= f.hidden_field :luv, value: @artist.id %>
 <%= f.hidden_field :luvs, value: current_fan.id %>
 <%= f.submit "GIVE US SOME LUV ♥", class: "btn luv_btn" %>

<% end %>
